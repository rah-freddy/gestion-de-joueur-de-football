{% extends 'base.html.twig' %}

{% block title %}Nouveau équipe{% endblock %}

{% block body %}

<div class="container">
    <h3>Ajouter nouveau équipe (club)</h3>
    {{form_start(form)}}
    <div class="control-group">
        <div class="controls">
            {{ form_label(form.name) }}
            {{ form_widget(form.name, {'attr':{'class':'form-control'}}) }}
        </div>
    </div>
    <br>
    <div class="control-group">
        <div class="controls">
            <button class="btn btn-primary">
                Ajouter <ion-icon name="save-outline"></ion-icon>
            </button>
        </div>
    </div>
    {{form_end(form)}}
</div>

{% endblock %}